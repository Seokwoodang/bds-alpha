# Review — 부동산알파 · Round 1
reviewer: code-reviewer (blind — 현재 코드 + resolved spec만, 작성자 맥락 없음)   suite: vitest 47/47 · e2e 28 pass/1 skip

독립 리뷰어가 전체 코드를 resolved-spec 대비 검토해 17건 발견. 아래는 **각 발견 + 권장 disposition**(사용자가 override 가능). 높은 확신 항목은 이번 라운드에서 선반영(사용자 위임 "계속해"에 따름) — r2에서 closure 확인.

## 발견 + disposition

### 🔴 Critical
- **R1 · PostgREST `.or()` 주입(쉼표/중괄호)** `listingsQuery.ts:40` — q에 `,` `(` `)` `{` `}` 포함 시 or() 문자열이 깨져 400/오결과/DoS. → **fix**: q에서 해당 문자 제거(정규화).
- **R2 · 로딩 스켈레톤 부재(C25)** — `loading.tsx`/`Suspense`/Skeleton 없음. force-dynamic라 데이터 대기 중 빈 화면. → **fix**: 데이터 라우트에 `loading.tsx` + SkeletonCard 추가.
- **R3 · 하트 토글 후 헤더 savedCount 동기(C46)** `HeartButton.tsx` — 서버액션 후 명시적 `router.refresh()` 없음(서버액션 자동 리렌더에 의존). → **fix**: 안전하게 `router.refresh()` 추가.

### 🟡 Major
- **R4 · tags 검색이 `cs`(요소 정확일치)라 부분일치 아님(Decision 7)** — 배열에 ilike 부분일치는 PostgREST .or()로 불가. → **fix(문서화)+defer**: tags는 "완전 태그 일치"로 동작(부분-태그 검색은 F7로 deferred, RPC/생성컬럼 필요). 한 태그명 검색은 정상(검증됨).
- **R5 · 상담 신청 버튼 무반응(AD12: 토스트 또는 비활성)** `detail/page.tsx:87` → **fix**: `disabled` + "(준비 중)" 라벨.
- **R6 · AuthForm `push` 후 `refresh` 경합** — *reject*: 루트 레이아웃 캐시(헤더 세션) 무효화를 위해 refresh 필요. T77로 동작 검증됨. 현 상태 유지.
- **R7 · `getSaved`가 'use server' 파일에 있어 액션 엔드포인트로 노출** → **fix**: 읽기 전용 `getSaved`를 `savedRead.ts`(no directive)로 분리, `toggleSave`만 액션 유지.
- **R8 · GuideOverlay `onClose` 인라인 → effect 재실행 위험** → **fix**: GuidesView/HomeGuides에서 `useCallback`.
- **R9 · `Number('')===0`로 id=0 불필요 쿼리** `detail/page.tsx` → **fix**: `id<=0` 가드.
- **R10 · BackLink `history.length` 외부 referrer 오작동** → **fix**: `document.referrer` same-origin 확인.
- **R11 · `ord` 컬럼이 spec 스키마/타입에 없음** → **fix(문서화)**: design 스키마에 `ord` 명시 + Region 타입 주석.

### 🔵 Minor
- **R12 · 오버레이 backdrop role 없음/article에 role=dialog** → **fix**: backdrop `role=presentation`, dialog는 `div role=dialog` + `aria-labelledby`.
- **R13 · 비로그인 시 헤더 aria "관심목록 0개"** → **fix**: 비로그인은 "마이페이지".
- **R14 · 레이아웃+getSaved 이중 `auth.getUser()`** → **fix**: 레이아웃에서 인라인 조회.
- **R15 · listings/error.tsx `error` 미사용** → **fix**: dev에서 error.message 노출.
- **R16 · 마이페이지 '최근 본 지역' 하드코딩 강남구** → **fix**: '—'(추적 없음, AD8).
- **R17 · PriceChart `preserveAspectRatio="none"`** — *reject*: 원본 디자인 명세(viewBox 760×300, none). 의도된 동작. 유지.

### ✅ 잘된 점(리뷰어 확인)
- toggleSave의 user_id는 항상 검증된 세션에서만 → RLS와 함께 타 사용자 데이터 변경 불가.
- returnTo/next 오픈리다이렉트 방지 일관(`/`로 시작 & `//` 아님).
- buildListingsQuery 순수성(Supabase/React/Next import 없음) — 레이어 분리 준수.
- id 안정 2차 정렬, parseListingParams 기본값 정규화, @supabase/ssr 미들웨어 패턴 정확.
- 커밋 소스에 비밀키 없음(.env.local만, gitignore).

**요약:** 반드시 고칠 것 = R1(주입)·R2(스켈레톤)·R3(헤더 동기). R4·R11은 문서화/deferred. R6·R17은 reject(검증/명세). 나머지는 품질 개선으로 선반영.
