# Review — 부동산알파 · Round 2
suite: vitest 47/47 · e2e 30 pass/1 skip(컴포넌트로 이전) · tsc/build clean (프로덕션 빌드, 라이브 Supabase)

## 🔵 Closure of Round 1 (main loop — NOT blind)
r1 발견에 대한 disposition 적용 결과. (라이브 e2e/유닛으로 재검증)

| R1 | disposition | landed? | evidence |
|----|-------------|---------|----------|
| R1 `.or()` 주입(쉼표/중괄호) | fix | ✅ | `listingsQuery.ts` q 정규화(`[,(){}*]`→공백). q='강남,역삼' → 에러 없이 빈상태(라이브 검증) |
| R2 로딩 스켈레톤(C25) | fix | ✅ | `Skeleton.tsx` + `listings/loading.tsx`·`prices/loading.tsx` + bdsShimmer |
| R3 하트 토글 후 헤더 동기 | fix→**대체** | ✅ | 처음엔 router.refresh 추가했으나 transition pending 고착 → **낙관적 로컬 상태**(HeartButton)로 대체. T55 라이브 통과(aria-pressed 즉시 토글, 키보드 동작). 영속은 revalidatePath로 서버 동기 |
| R4 tags 부분일치 | fix(문서)+defer | ✅ | tags=완전일치(cs) 유지, 부분일치는 **F7**(RPC/생성컬럼). 완전 태그 검색 라이브 검증(한강뷰→2) |
| R5 상담 버튼 무반응(AD12) | fix | ✅ | `disabled` + "(준비 중)" 라벨 |
| R6 push 후 refresh 경합 | reject | n/a | 루트 레이아웃 캐시(세션) 무효화 필요. T77 로그인→returnTo→헤더 전환 라이브 통과 |
| R7 getSaved 액션 노출 | fix | ✅ | `savedRead.ts`(no 'use server')로 분리, 전 import 갱신. toggleSave만 액션 |
| R8 onClose 인라인 effect 재실행 | fix | ✅ | GuidesView/HomeGuides `useCallback` |
| R9 id=0 불필요 쿼리 | fix | ✅ | `id<=0` 가드 |
| R10 BackLink 외부 referrer | fix | ✅ | 외부 referrer만 push, 그외 back(client-nav referrer 공백 케이스 처리). T68 라이브 통과(필터 URL 보존) |
| R11 ord 컬럼 미문서 | fix(문서) | ✅ | design 스키마 + Region 타입 주석에 ord 명시 |
| R12 오버레이 backdrop role | fix | ✅ | backdrop `role=presentation` |
| R13 비로그인 헤더 aria | fix | ✅ | 비로그인 "마이페이지" |
| R14 이중 getUser | fix | ✅ | layout에서 인라인 조회 |
| R15 error.tsx error 미사용 | fix | ✅ | dev에서 error.message 노출 |
| R16 마이페이지 최근지역 하드코딩 | fix | ✅ | '—'(AD8, 추적 없음) |
| R17 preserveAspectRatio none | reject | n/a | 원본 디자인 명세(viewBox 760×300, none) — 의도된 동작 |

**reject 2건(R6·R17)은 명세/검증 근거로 유지, 나머지 15건 반영·재검증 완료.**

## Discovery (this round) — 추가 발견
이번 라운드는 r1 대량 수정 후 closure 중심. 라이브 e2e/유닛 전수 재실행에서 회귀 0. 추가 blocker/major 발견 없음.
- (참고) `notFound()` HTTP 상태가 프로덕션 force-dynamic 스트리밍에서 200으로 커밋되는 Next 한계 발견 → not-found 페이지는 정상 렌더(함수적 404). 상태코드는 **F8**로 deferred(SEO 영향, 미들웨어 레벨 처리 필요).

## 다음
원하면 **2차 blind 독립 리뷰**(fresh reviewer)를 한 번 더 돌려 교차검증 가능. 아니면 본 라운드 승인 시 reviewApproved → 종합검증(Phase 11)·Gate 2로 진행.
